Read the JSON review file below carefully:

C:\reandy\ultimate-screener\app\artifacts\secret-sauce-review\secret-sauce-walkforward-review-2026-04-20T16-44-04Z.json

Your task is to revise the Secret Sauce formula based on:
1. the 10-day walk-forward results
2. the historical pre-breakout fingerprint
3. the currently active Secret Sauce formula
4. the excessive number of false positives
5. the still-low number of true hits

Primary objective:
- improve precision first
- reduce weak candidates / false positives
- keep the formula aligned with the pre-breakout behavior seen in real top gainers

What to analyze:
- compare the active formula with the best-looking walk-forward formulas
- identify thresholds that are too loose
- identify thresholds that may still be too strict, if any
- review volume, distance to EMA20, compression, RSI, MFI, and close-near-high behavior
- suggest new filters if needed
- suggest removing filters if they are harmful

Rules for the answer:
- do not give general advice
- all threshold changes must be numeric and explicit
- every threshold change must have a reason
- prioritize precision over candidate count
- explain tradeoffs briefly when needed
- return ONLY valid JSON

Return JSON in this exact shape:

{
  "verdict": "tighten|loosen|keep",
  "why": "short summary of the main reason",
  "thresholdChanges": [
    {
      "field": "minRvol|maxDistEma20Pct|maxCompressionPct|minCloseNearHighPct|minRsi|maxRsi|minMfi|minDistEma20Pct|targetGainPct|stopLossPct|holdingDays|lookbackCandles",
      "current": 0,
      "proposed": 0,
      "reason": "why this change is needed"
    }
  ],
  "addFilters": [
    "optional suggested new filter"
  ],
  "removeFilters": [
    "optional filter to remove"
  ],
  "notes": "important extra observations"
}

Save the response to:

C:\reandy\ultimate-screener\app\artifacts\secret-sauce-review\manual-revision-walkforward.json

Then apply it with:

npm run secret-sauce:apply-revision -- "artifacts/secret-sauce-review/manual-revision-walkforward.json"
